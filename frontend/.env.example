# Token API — deve coincidere con API_TOKEN nel .env del backend.
# Default (se .env manca): local-dev-token-2026
VITE_API_TOKEN=local-dev-token-2026

# URL del backend in dev (Vite proxy /api → qui). Default: http://127.0.0.1:8000
# VITE_BACKEND_URL=http://127.0.0.1:8000
